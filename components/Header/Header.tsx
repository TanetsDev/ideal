import React from "react";
import MainContainer from "../Containers/MainContainer";

const Header = () => {
  return (
    <header>
      <MainContainer>Header</MainContainer>
    </header>
  );
};

export default Header;
